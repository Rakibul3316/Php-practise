<?php

// একাধিক সংখ্যা যোগ করার ফাংশন:-

// এখানে int ব্যবহার করা হয়েছে যাতে int ছাড়া অন্য কোন টাইপের ডেটা ইনপুট করলে ইরোর দেখায়। আমরা চাইলে ফাংশন যেটা return করবে তার ডেটা টাইপ নির্দিষ্ট করে দিতে পারি। 

function sum (int $x, int $y, int $z): int {
    return $x+$y+$z;
}

echo sum (5,6,7);